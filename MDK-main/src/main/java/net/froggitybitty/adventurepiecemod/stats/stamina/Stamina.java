package net.froggitybitty.adventurepiecemod.stats.stamina;

import net.minecraft.network.chat.Component;
import net.minecraft.world.entity.player.Player;

import static net.froggitybitty.adventurepiecemod.stats.DataAttachmentProvider.*;

public class Stamina {
    //public static int stamina;
    //public static int maxStamina;
    //public static int regenPerSec;
//
    //public static int setMaxStamina(Player player){
    //    maxStamina = player.getData(MAX_STAMINA) + (player.getData(ENDURANCE_STAT) * 2);
    //    return maxStamina;
    //}
    //public static void subStamina(Player player, int sub){
    //    player.setData(STAMINA, player.getData(STAMINA) - sub);
    //}
    //public static void addStamina(Player player, int add){
    //    player.setData(STAMINA, player.getData(STAMINA) + add);
    //}
    //public static int staminaRegenPerSec(Player player){
    //    if (player.getData(ENDURANCE_STAT) < 100){
    //        regenPerSec = 1;
    //        return regenPerSec;
    //    }
    //    if (player.getData(ENDURANCE_STAT) >= 100 && player.getData(ENDURANCE_STAT) < 200){
    //        regenPerSec = 2;
    //        return regenPerSec;
    //    }
    //    if (player.getData(ENDURANCE_STAT) >= 200 && player.getData(ENDURANCE_STAT) < 300){
    //        regenPerSec = 3;
    //        return regenPerSec;
    //    }
    //    if (player.getData(ENDURANCE_STAT) == 300){
    //        regenPerSec = 4;
    //        return regenPerSec;
    //    }
    //    return regenPerSec;
    //}
    //public static void staminaRegen(Player player) throws InterruptedException {
    //    for (stamina = player.getData(STAMINA);
    //         stamina < player.getData(MAX_STAMINA);
    //         player.setData(STAMINA, player.getData(STAMINA) + 1)){
    //        Object obj = new Object();
    //        obj.wait(1000 / staminaRegenPerSec(player));
    //        player.displayClientMessage(Component.literal("Stamina: " + player.getData(STAMINA)), true);
    //    }
    //}
}
